/**
 * The npm scope shared by every package in this project. Kept as a single
 * exported constant so the whole project can be renamed with one edit here
 * plus a find/replace, per the project's rename-ability constraint.
 */
export const SCOPE = '@mailway';
