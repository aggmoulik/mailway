# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `pnpm changeset` to record a version bump for a package. On release, changesets
consumes these files to bump versions and generate changelogs.
